// OSS stub for comparing reminder body to template

export default function compareReminderBodyToTemplate(_reminderBody: string, _template: string): boolean {
  // OSS stub - always returns true (no comparison)
  // In production, this would compare the reminder body to a template
  return true;
}
